Description: Downloads the latest Blender MSI installer (x64) and imports into Cimian.
Identifier: com.github.autopkg.cimian.Blender-x64
MinimumVersion: "2.7.0"
Input:
  NAME: Blender
  ARCHITECTURE: x64

Process:
  # Blender's MSI ships with an empty ProductVersion property, which makes
  # CimianInfoCreator fall back to 0.0.0. Capture the real version from
  # the download URL (e.g. blender-4.5.1-windows-x64.msi) and pass it in
  # explicitly via cimian_info_version.
  - Processor: URLTextSearcher
    Arguments:
      url: https://www.blender.org/download/
      re_pattern: '(?P<match>https://[^"]*/blender-(?P<version>[0-9]+\.[0-9]+(?:\.[0-9]+)?)-windows-x64\.msi)'
      result_output_var_name: msi_url

  - Processor: URLDownloader
    Arguments:
      url: "%msi_url%"
      filename: "%NAME%-%ARCHITECTURE%.msi"

  - Processor: EndOfCheckPhase

  - Processor: CimianInfoCreator
    Arguments:
      pkg_path: "%pathname%"
      installer_type: msi
      cimian_info_name: "%NAME%"
      cimian_info_version: "%version%"
      cimian_info_category: Modeling
      cimian_info_developer: Blender Foundation
      cimian_info_icon_name: "%NAME%.png"
      cimian_info_description: >
        Blender is a free and open source 3D creation suite supporting the
        entirety of the 3D pipeline — modelling, rigging, animation,
        simulation, rendering, compositing and motion tracking.

  - Processor: CimianImporter
    Arguments:
      pkg_path: "%pathname%"
      installer_type: msi
      cimian_subdirectory: apps/modeling
      supported_architectures:
        - "%ARCHITECTURE%"
      catalogs:
        - Development
        - Testing
      unattended_install: true
      unattended_uninstall: true
